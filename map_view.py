import plotly.graph_objects as go


def create_route_figure(
    route,
    start_id,
    goal_id,
    places,
    roads,
    place_by_id,
    food_places,
):
    figure = go.Figure()

    # 現在のcreate_route_figure関数の内容をここへ移す

    return figure
